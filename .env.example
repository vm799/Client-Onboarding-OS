# ===========================================
# CLIENT ONBOARDING OS - Environment Variables
# ===========================================
# Copy this file to .env.local and fill in your values

# ===========================================
# SUPABASE (Required)
# ===========================================
# Get these from: https://supabase.com/dashboard/project/YOUR_PROJECT/settings/api
NEXT_PUBLIC_SUPABASE_URL=your-supabase-url
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-supabase-anon-key
SUPABASE_SERVICE_ROLE_KEY=your-supabase-service-role-key

# ===========================================
# APPLICATION (Required)
# ===========================================
# The public URL where your app is deployed (no trailing slash)
# Local development: http://localhost:3000
# Production: https://yourdomain.com
NEXT_PUBLIC_APP_URL=http://localhost:3000

# ===========================================
# EMAIL - RESEND (Optional but Recommended)
# ===========================================
# Get from: https://resend.com/api-keys
# Without this, emails are logged to console only
RESEND_API_KEY=your-resend-api-key
EMAIL_FROM=onboarding@yourdomain.com

# ===========================================
# CRON JOBS (Required for automated reminders)
# ===========================================
# Secret to protect cron endpoints from unauthorized access
# Generate a random string: openssl rand -hex 32
CRON_SECRET=your-cron-secret-here

# ===========================================
# DEPLOYMENT NOTES
# ===========================================
# For Vercel deployment:
# 1. Add all env vars in Vercel dashboard
# 2. Set up cron job for reminders by creating vercel.json:
#    {
#      "crons": [{
#        "path": "/api/cron/send-reminders",
#        "schedule": "0 9 * * *"
#      }]
#    }
# 3. Run database migrations via Supabase dashboard
